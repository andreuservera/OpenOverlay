namespace IRacingOverlay.App.ViewModels;

/// <summary>Visual theme for the fullscreen Dashboard. Floating widgets always use the Classic look
/// (they never merge a theme dictionary of their own) — this only affects the Dashboard window.</summary>
public enum DashboardTheme
{
    Classic,
    DigitalHud,
    RawDiy,
}
